"""Supplemental protocol experiments for the classical SQI reproduction.

This package is intentionally outside ``src/sqi_pipeline``.  It may import the
paper-aligned pipeline utilities, but it keeps all extra protocol audits,
reruns, tables, and figures in ``outputs/sqi_supplemental`` and
``outputs/reports/sqi_supplemental``.
"""

from __future__ import annotations

__all__ = ["__version__"]

__version__ = "0.1.0"
